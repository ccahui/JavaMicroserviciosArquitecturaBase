server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ccahui/JavaMicroserviciosArquitecturaBase.git
          searchPaths: config-data-properties
          default-label: "main"